import re

# =============================================================================
# Advanced Masking Rules (Regex)
# =============================================================================
# ここに隠したいパターンの正規表現を記述してください。
# OCRエンジンが文字の間にスペースを入れることがあるため、 \s* を活用することをお勧めします。

USER_PATTERNS = [
    # 例: 「秘密プロジェクト」という文字を、間にスペースがあっても隠す場合
    # re.compile(r'秘\s*密\s*プ\s*ロ\s*ジ\s*ェ\s*ク\s*ト'),
    
    # 例: 特定の社員番号形式 (ID-12345) を隠す場合
    # re.compile(r'ID\s*-\s*\d{5}'),
]
